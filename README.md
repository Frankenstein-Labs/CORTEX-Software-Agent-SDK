# Cortex AI Engine

Independent orchestration engine for autonomous software engineering agents.

## Current implementation

The first executable Core slice is implemented in TypeScript. It includes versioned protocol events, an in-memory Event Bus, a dependency-aware Task Graph, and policy enforcement for tools, paths, budgets, and destructive operations. Run `npm test` to build and execute the unit tests.
