# `@cortex/engine-adapters`

Neutral adapter contracts for external agent and design engines. OpenHands and OpenDesign are integration targets, not owners of Cortex Core. Concrete adapters belong in dedicated subdirectories and must translate all engine-specific types at this boundary.
