# `@cortex/runtime`

Runtime abstraction for controlled command execution and file operations. `LocalHostRuntime` is the first implementation and confines working directories to a configured workspace root.
